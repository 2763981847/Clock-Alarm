/**
 * 世界时钟常量类。
 */
export default class WorldClockConstants {
  /**
   * 计时器格式。
   */
  static readonly TIMER_FORMAT: string = 'HH:mm';
  /**
   * 世界时钟字符串。
   */
  static readonly WORLD_CLOCK: string = '世界时钟';
}
